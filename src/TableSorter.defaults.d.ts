import { ITableSorterSettings } from "./models";
/**
 * The default numbering format to use when formatting numbers to display in lineup
 */
export declare const DEFAULT_NUMBER_FORMATTER: (n: number) => string;
/**
 * Represents the settings
 */
export declare const DEFAULT_TABLESORTER_SETTINGS: ITableSorterSettings;
