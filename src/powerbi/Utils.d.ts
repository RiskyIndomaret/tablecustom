/**
 * Creates a calculator that calculates an appropriate date string format to represent the data
 */
export declare function dateTimeFormatCalculator(): {
    addToCalculation(date: Date): void;
    getFormat(): string;
};
